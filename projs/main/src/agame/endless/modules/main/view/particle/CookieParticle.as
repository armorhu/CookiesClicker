package agame.endless.modules.main.view.particle
{
	import starling.extensions.PDParticleSystem;
	import starling.textures.Texture;

	public class CookieParticle extends PDParticleSystem
	{
		public function CookieParticle(config:XML, texture:Texture)
		{
			super(config, texture);
		}

		public function add():void
		{
//			if (mNumParticles == capacity)
//				raiseCapacity(capacity);
//
//			particle=mParticles[mNumParticles] as Particle;
//			initParticle(particle);

		}
	}
}
